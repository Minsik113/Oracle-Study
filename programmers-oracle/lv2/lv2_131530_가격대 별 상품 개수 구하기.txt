-- 코드를 입력하세요 PRICE_GROUP
SELECT TO_NUMBER(SUBSTR(PRICE,0,LENGTH(PRICE)-4) || '0000') "PRICE_GROUP"
    , COUNT(*) PRODUCTS 
FROM PRODUCT
GROUP BY SUBSTR(PRICE,0,LENGTH(PRICE)-4)
ORDER BY SUBSTR(PRICE,0,LENGTH(PRICE)-4)
;