-- 코드를 입력하세요
-- USER_ID PRODUCT_ID SALES_DATE 셋 다 같은건 없다
-- ID 오른차순, 상품ID 내림차순
SELECT A.USER_ID, A.PRODUCT_ID
FROM (SELECT USER_ID, PRODUCT_ID, COUNT(*) TOTAL
    FROM ONLINE_SALE
    GROUP BY USER_ID, PRODUCT_ID
    ORDER BY USER_ID ASC, PRODUCT_ID DESC) A
WHERE A.TOTAL >= 2
;