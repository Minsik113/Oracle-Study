-- 
SELECT O.ANIMAL_ID
        ,O.ANIMAL_TYPE
        ,O.NAME
FROM ANIMAL_OUTS O, ANIMAL_INS I
WHERE O.ANIMAL_ID = I.ANIMAL_ID(+)
    AND I.ANIMAL_ID IS NOT NULL -- 보호소 나가는거 까지 
    AND I.SEX_UPON_INTAKE LIKE ('I%')
    AND O.SEX_UPON_OUTCOME <> I.SEX_UPON_INTAKE
ORDER BY O.ANIMAL_ID
;