-- 코드를 입력하세요
-- 1.2022-01에 해당하는 데이터 조회
-- 2.카테고리로 GROUP BY, SUM()으로 매출액(판매량X판매가) 구하기
-- 3. 정렬
SELECT A.AUTHOR_ID
    ,A.AUTHOR_NAME
    ,B.CATEGORY
    ,SUM(C.SALES*B.PRICE) TOTAL_SALES
FROM AUTHOR A, BOOK B, BOOK_SALES C
WHERE TO_CHAR(C.SALES_DATE,'YYYY-MM') = '2022-01'
    AND A.AUTHOR_ID = B.AUTHOR_ID
    AND B.BOOK_ID = C.BOOK_ID
GROUP BY A.AUTHOR_ID, A.AUTHOR_NAME, B.CATEGORY
ORDER BY A.AUTHOR_ID ASC, B.CATEGORY DESC
;