-- 코드를 입력하세요
-- 1.CATEGORY로 GROUP BY 한다.
-- 2.모든 행의 SALES를 SUM한다
SELECT A.CATEGORY CATEGORY
    ,SUM(B.SALES) TOTAL_SALES
FROM BOOK A, BOOK_SALES B
WHERE A.BOOK_ID = B.BOOK_ID
    AND TO_CHAR(B.SALES_DATE,'YYYY-MM') = '2022-01'
GROUP BY A.CATEGORY
ORDER BY A.CATEGORY
;